package com.company;

public class Main {
    public static void main(String[] args) {//only start and stop the program, see GameHost java for the whole procedure
        GameHost host=new GameHost();
        host.startTheGame();
    }
}
